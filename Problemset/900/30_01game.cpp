#include <bits/stdc++.h>
using namespace std; 

void solve(string s) {

}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while(t--) {
        string s;
        cin>>s;
        solve(s);
    }
    return 0;
}